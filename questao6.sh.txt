#!/bin/bash
echo -e ";&\nContinua a execução com a lista associada com o próximo conjunto de padrões "
echo -e ";;&\nFaz com que o shell teste o próximo padrão da lista como parâmetro, com as listas seguintes, se a lista não for idêntica a saída é 0, caso contrário o status da saída é do último comando executado na lista"
